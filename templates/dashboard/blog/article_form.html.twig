{% extends 'adminBase.html.twig' %}{% block title %}{{ creation ? 'Ajouter un article' : 'Modifier un article' }}{% endblock %}{% block body %}<section class="content-manager content-manager--form"><header class="content-manager__header"><div><h1>{{ creation ? 'Ajouter un article' : 'Modifier un article' }}</h1><p>Un brouillon peut être incomplet. Les champs requis seront contrôlés lors de sa soumission en relecture.</p></div></header>{% include 'dashboard/_form.html.twig' with {form: form, cancel_path: path('app_dashboard_blog'), dropzone_fields: ['coverImageFile'], dropzone_current_images: {coverImageFile: article ? article.coverImage : null}} only %}</section>{% endblock %}
{# Blog article form. #}
