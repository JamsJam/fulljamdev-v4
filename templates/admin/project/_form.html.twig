{{ form_start(form) }}
    <input type="hidden" name="step" value="{{step + 1}}">
    {# <input type"hidden" name="crsf" value="{{csrf_token()}}"- #}
    {% if step == 1 %}
    
        {{form_row(form.title)}}
        {{form_row(form.description)}}
        {# {{form_row(form.teschnologies)}} #}
        {# {{form_row(form.image)}} #}
        {{form_row(form.isOnline)}}
        {{form_row(form.isGitpublic)}}
        {{form_row(form.gitlink)}}
        {{form_row(form.projectlink)}}
        {# {{form_row(form.tags)}} #}
    {% endif %}

    {% if step == 2 %}
    
        {{form_row(form.casestudy)}}

    
    {% endif %}

    {% if step == 3 %}
        {{form_row(form.metaDescription)}}
        {{form_row(form.opengraph)}}
        {# {{form_row(form.)}}
        {{form_row(form.)}} #}
    
    
    {% endif %}
    
    {# {{ form_widget(form) }} #}
    {% if step > 1 %}
        <a class="button__componnent" href="">Précédent</a>
    {% endif %}

    {% if step < 3 %}
        <button class="button__componnent">{{ button_label|default('Suivant') }}</button>
    {% endif %}

    
    {% if step == 3 %}
        <button class="button__componnent">{{ button_label|default('Enregistrer') }}</button>
    {% endif %}
    
{{ form_end(form) }}
