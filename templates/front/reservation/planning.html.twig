{% extends 'base.html.twig' %}

{% block title %}Réserver — {{ planning.title }}{% endblock %}
{% block robots %}{{ planning.online ? parent() : 'noindex, nofollow' }}{% endblock %}

{% block body %}
    <turbo-frame id="{{ booking_frame }}">
        <section id="{{ booking_target }}" class="public-booking" {{ stimulus_controller('public-booking') }}>
            {% include 'front/reservation/partial/_booking.html.twig' %}
        </section>
    </turbo-frame>
{% endblock %}
