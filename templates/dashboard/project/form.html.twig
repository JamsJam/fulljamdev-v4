{% extends 'adminBase.html.twig' %}{% block title %}{{ creation ? 'Ajouter un projet' : 'Modifier un projet' }}{% endblock %}{% block body %}<section class="content-manager content-manager--form"><header class="content-manager__header"><div><h1>{{ creation ? 'Ajouter un projet' : 'Modifier un projet' }}</h1><p>Le projet est enregistré comme brouillon et son slug est généré automatiquement.</p></div></header>{% include 'dashboard/_form.html.twig' with {form: form, cancel_path: path('app_dashboard_project'), dropzone_fields: ['imageFiles'], dropzone_current_image_lists: {imageFiles: project ? project.images|map(image => image.path) : []}} only %}</section>{% endblock %}
{# Project form. #}
